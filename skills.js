function calculateNumbers(var1, var2)          // Function declaration
{
    var total = var1 + var2;                      // Function definition
    return total;                                 // Function returns the total
}